# Convert Schema to Plain List

Internal function to convert z_schema to plain list, stripping class
attributes.

## Usage

``` r
schema_to_list(schema)
```

## Arguments

- schema:

  A z_schema object.

## Value

A plain list suitable for JSON conversion.
