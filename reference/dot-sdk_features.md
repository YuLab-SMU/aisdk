# SDK Feature Flags

Global feature flags for controlling SDK behavior. These flags allow
gradual migration to new features while maintaining backward
compatibility.

## Usage

``` r
.sdk_features
```

## Format

An object of class `environment` of length 8.
