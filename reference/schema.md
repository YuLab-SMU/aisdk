# Schema DSL: Lightweight JSON Schema Generator

A lightweight DSL (Domain Specific Language) for defining JSON Schema
structures in R, inspired by Zod from TypeScript. Used for defining tool
parameters.
