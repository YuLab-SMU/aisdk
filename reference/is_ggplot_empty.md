# Check if Value is Empty (ggplot2 semantics)

Mirrors ggplot2's internal `empty()` function behavior. Returns TRUE if:
NULL, 0 rows, 0 cols, or waiver object.

## Usage

``` r
is_ggplot_empty(x)
```

## Arguments

- x:

  Value to check.

## Value

Logical.
