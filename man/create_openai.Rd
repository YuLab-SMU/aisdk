% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provider_openai.R
\name{create_openai}
\alias{create_openai}
\title{Create OpenAI Provider}
\usage{
create_openai(
  api_key = NULL,
  base_url = NULL,
  organization = NULL,
  project = NULL,
  headers = NULL,
  name = NULL,
  timeout_seconds = NULL,
  total_timeout_seconds = NULL,
  first_byte_timeout_seconds = NULL,
  connect_timeout_seconds = NULL,
  idle_timeout_seconds = NULL,
  disable_stream_options = FALSE
)
}
\arguments{
\item{api_key}{OpenAI API key. Defaults to OPENAI_API_KEY env var.}

\item{base_url}{Base URL for API calls. Defaults to https://api.openai.com/v1.}

\item{organization}{Optional OpenAI organization ID.}

\item{project}{Optional OpenAI project ID.}

\item{headers}{Optional additional headers.}

\item{name}{Optional provider name override (for compatible APIs).}

\item{timeout_seconds}{Legacy alias for \code{total_timeout_seconds}.}

\item{total_timeout_seconds}{Optional total request timeout in seconds for API calls.}

\item{first_byte_timeout_seconds}{Optional time-to-first-byte timeout in seconds for API calls.}

\item{connect_timeout_seconds}{Optional connection-establishment timeout in seconds for API calls.}

\item{idle_timeout_seconds}{Optional stall timeout in seconds for API calls.}

\item{disable_stream_options}{Disable stream_options parameter (for providers like Volcengine that don't support it).}
}
\value{
An OpenAIProvider object.
}
\description{
Factory function to create an OpenAI provider.
}
\section{Supported Models}{

\itemize{
\item \strong{gpt-5.5}: OpenAI's latest flagship (April 2026). First full retrain since GPT-4.5. Agentic coding, 1M context, computer use. (Reasoning, Vision, Tools, Structured) | ctx: 1000k
\item \strong{gpt-5.5-pro}: Research-grade variant of GPT-5.5 with maximum accuracy. (Reasoning, Vision, Tools, Structured) | ctx: 1000k
\item \strong{gpt-5.4}: Previous flagship (March 2026). Codex genes, native Computer Use, Tool Search. (Reasoning, Vision, Tools, Structured) | ctx: 1000k
\item \strong{gpt-5.4-mini}: Faster GPT-5.4 variant optimized for coding, computer use, and subagents. (Reasoning, Vision, Tools, Structured) | ctx: 1000k
\item \strong{gpt-5.4-nano}: Smallest and cheapest GPT-5.4 for lightweight tasks. (Vision, Tools, Structured) | ctx: 1000k
\item \strong{gpt-5.3-codex}: Agentic coding specialist. Terminal-Bench 77.3\%. General tasks supported. (Reasoning, Vision, Tools, Structured) | ctx: 200k
\item \strong{gpt-4.1}: Production workhorse with 1M token context window. (Vision, Tools, Structured) | ctx: 1000k
\item \strong{gpt-4.1-mini}: Long-context budget model with 1M token window. (Vision, Tools, Structured) | ctx: 1000k
\item \strong{gpt-4.1-nano}: Ultra-low-cost model for classification, routing, and simple tasks. (Vision, Tools, Structured) | ctx: 1000k
\item \strong{gpt-4o}: Legacy flagship multimodal model with wide ecosystem support. (Vision, Tools, Structured) | ctx: 128k
\item \strong{gpt-4o-mini}: Budget-friendly general-purpose model. (Vision, Tools, Structured) | ctx: 128k
\item \strong{o3}: Advanced reasoning model for multi-step problem solving. (Reasoning, Vision, Tools, Structured) | ctx: 200k
\item \strong{o4-mini}: Budget reasoning model for math, logic, and code review. (Reasoning, Vision, Tools, Structured) | ctx: 200k
\item \strong{o3-mini}: Lightweight reasoning model for moderate reasoning tasks. (Reasoning, Vision, Tools, Structured) | ctx: 200k
\item \strong{o1}: Premium reasoning model for research-grade problem solving. (Reasoning, Vision, Structured) | ctx: 200k
\item \emph{... and 2 more models. Use \code{list_models("openai")} to see all.}
}
}

\section{Token Limit Parameters}{

The SDK provides a unified \code{max_tokens} parameter that automatically maps to the
correct API field based on the model and API type:

\itemize{
\item \strong{Chat API (standard models)}: \code{max_tokens} -> \code{max_tokens}
\item \strong{Chat API (o1/o3 models)}: \code{max_tokens} -> \code{max_completion_tokens}
\item \strong{Responses API}: \code{max_tokens} -> \code{max_output_tokens} (total: reasoning + answer)
}

For advanced users who need fine-grained control:
\itemize{
\item \code{max_completion_tokens}: Explicitly set completion tokens (Chat API, o1/o3)
\item \code{max_output_tokens}: Explicitly set total output limit (Responses API)
\item \code{max_answer_tokens}: Limit answer only, excluding reasoning (Responses API, Volcengine-specific)
}
}

\examples{
\donttest{
if (interactive()) {
  # Basic usage with Chat Completions API
  openai <- create_openai(api_key = "sk-...")
  model <- openai$language_model("gpt-4o")
  result <- generate_text(model, "Hello!")

  # Using Responses API for reasoning models
  openai <- create_openai()
  model <- openai$responses_model("o1")
  result <- generate_text(model, "Solve this math problem...")
  print(result$reasoning) # Access chain-of-thought

  # Smart model selection (auto-detects best API)
  model <- openai$smart_model("o3-mini") # Uses Responses API
  model <- openai$smart_model("gpt-4o") # Uses Chat Completions API

  # Token limits - unified interface
  # For standard models: limits generated content
  result <- model$generate(messages = msgs, max_tokens = 1000)

  # For o1/o3 models: automatically maps to max_completion_tokens
  model_o1 <- openai$language_model("o1")
  result <- model_o1$generate(messages = msgs, max_tokens = 2000)

  # For Responses API: automatically maps to max_output_tokens (total limit)
  model_resp <- openai$responses_model("o1")
  result <- model_resp$generate(messages = msgs, max_tokens = 2000)

  # Advanced: explicitly control answer-only limit (Volcengine Responses API)
  result <- model_resp$generate(messages = msgs, max_answer_tokens = 500)

  # Multi-turn conversation with Responses API
  model <- openai$responses_model("o1")
  result1 <- generate_text(model, "What is 2+2?")
  result2 <- generate_text(model, "Now multiply that by 3") # Remembers context
  model$reset() # Start fresh conversation
}
}
}
