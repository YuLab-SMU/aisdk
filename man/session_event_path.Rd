% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/session_event_store.R
\name{session_event_path}
\alias{session_event_path}
\title{Path of a Session's JSONL Event Log}
\usage{
session_event_path(session = NULL, startup_dir = getwd())
}
\arguments{
\item{session}{A \code{ChatSession} object, or \code{NULL}.}

\item{startup_dir}{Directory whose \verb{.aisdk/sessions/} subdirectory holds
session event logs when the session does not carry an explicit store
root. Defaults to \code{getwd()}.}
}
\value{
The event-log file path (character scalar).
}
\description{
Resolves (and creates, if needed) the store directory and returns the
path of the session's event log file.
}
\keyword{internal}
