% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_api.R
\name{generate_batch}
\alias{generate_batch}
\title{Generate Text over a Batch of Prompts}
\usage{
generate_batch(prompts, model = NULL, concurrency = 4L, registry = NULL, ...)
}
\arguments{
\item{prompts}{A character vector or list of prompts.}

\item{model}{A model (id or object) used for every prompt.}

\item{concurrency}{Max worker processes (default 4). Effective on
Unix/macOS via \code{parallel::mclapply}; Windows and \code{concurrency = 1} run
sequentially.}

\item{registry}{Optional ProviderRegistry.}

\item{...}{Passed to every \code{generate_text()} call.}
}
\value{
A list the same length and order as \code{prompts}; each element is a
\code{GenerateResult}, or the error condition for a prompt that failed.
}
\description{
Run \code{generate_text()} for each prompt in a batch, concurrently where the
platform supports it, with per-item error isolation — one failing prompt
does not abort the batch. Useful for bulk work over a dataset (e.g.
classify/summarize each row) and pairs with \code{\link[=estimate_cost]{estimate_cost()}} for totals.
}
