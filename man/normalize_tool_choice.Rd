% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/content_translation.R
\name{normalize_tool_choice}
\alias{normalize_tool_choice}
\title{Normalize a portable \code{tool_choice} into a provider-specific shape}
\usage{
normalize_tool_choice(choice, target, parallel = NULL)
}
\arguments{
\item{choice}{The unified value (or a provider-native value to pass through).}

\item{target}{One of "openai_chat", "openai_responses", "anthropic", "gemini".}

\item{parallel}{Optional logical: when \code{FALSE}, request that the model not
emit parallel tool calls. Only Anthropic carries this inside \code{tool_choice}
(\code{disable_parallel_tool_use}); OpenAI uses a separate top-level
\code{parallel_tool_calls} and Gemini has no equivalent.}
}
\value{
The provider-shaped value to place at the provider's tool-choice
slot, or \code{NULL} when there is nothing to set.
}
\description{
A unified \code{tool_choice} (Vercel-AI-SDK style) that behaves the same across
providers. Each wire API spells this differently — OpenAI uses a top-level
\code{tool_choice} string/object, Anthropic a \code{{type: ...}} object, Gemini a
\code{toolConfig.functionCallingConfig.mode} — so hand-written provider-native
values do not port. This maps the unified forms to the target's shape and
passes anything it does not recognize through unchanged (so existing code
that already writes a provider-native value keeps working).
}
\details{
Unified forms:
\itemize{
\item \code{"auto"} — the model decides (provider default)
\item \code{"required"} / \code{"any"} — force the model to call some tool
\item \code{"none"} — forbid tool calls
\item \code{list(type = "tool", name = "X")} — force a specific tool
(also accepts \verb{toolName =} or a bare \code{list(tool = "X")})
}
}
\keyword{internal}
