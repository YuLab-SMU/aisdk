% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/session_event_store.R
\name{session_append_event}
\alias{session_append_event}
\title{Append an Event to a Session's Event Log}
\usage{
session_append_event(
  session,
  type,
  payload = list(),
  startup_dir = getwd(),
  visible = FALSE,
  branch_id = NULL
)
}
\arguments{
\item{session}{A \code{ChatSession} object.}

\item{type}{Event type string (e.g. \code{"message"}, \code{"branch"}).}

\item{payload}{Named list payload; non-serializable objects are reduced
to compact placeholders.}

\item{startup_dir}{Directory used to resolve the event log path.}

\item{visible}{Logical; marks events that represent user-visible
transcript entries.}

\item{branch_id}{Branch id to tag the event with; defaults to the
session's active branch.}
}
\value{
Invisibly, the event record (a list).
}
\description{
Serializes one event as a JSON line and appends it to the session's event
log. Events carry a type, the active branch id, a visibility flag, and an
arbitrary JSON-serializable payload.
}
\keyword{internal}
