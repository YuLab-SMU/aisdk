% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_http.R
\name{rate_limit_status}
\alias{rate_limit_status}
\title{Latest API Rate-Limit Status}
\usage{
rate_limit_status()
}
\value{
A list with \code{remaining_requests}, \code{remaining_tokens}, \code{limit_requests},
\code{limit_tokens}, \code{reset_requests}, \code{reset_tokens}, \code{retry_after}, \code{status},
\code{url}, and \code{timestamp}; or \code{NULL} when nothing has been captured.
}
\description{
Returns the most recent rate-limit snapshot parsed from a provider's response
headers, or \code{NULL} if none has been seen this session. Use it to pace a
request loop (e.g. slow down or pause when \code{remaining_requests} /
\code{remaining_tokens} runs low) instead of only reacting to a 429 after the fact.
OpenAI and Anthropic report these headers; Gemini does not.
}
