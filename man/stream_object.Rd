% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_object.R
\name{stream_object}
\alias{stream_object}
\title{Stream a Structured Object}
\usage{
stream_object(
  model = NULL,
  prompt,
  schema,
  schema_name = "result",
  on_partial = NULL,
  system = NULL,
  temperature = 0.3,
  max_tokens = NULL,
  registry = NULL,
  ...
)
}
\arguments{
\item{model}{A model (id or object).}

\item{prompt}{The input prompt.}

\item{schema}{A \code{z_schema} describing the desired object.}

\item{schema_name}{Name for the schema in the instruction.}

\item{on_partial}{Optional \verb{function(object, done)} called after each chunk
with the current best-effort parsed object (may be incomplete) and a
\code{done} flag on the final call.}

\item{system}{Optional system prompt.}

\item{temperature}{Sampling temperature (default 0.3).}

\item{max_tokens}{Optional max output tokens.}

\item{registry}{Optional ProviderRegistry.}

\item{...}{Passed to \code{stream_text()}.}
}
\value{
A \code{GenerateObjectResult} with the final parsed \code{object}.
}
\description{
Stream a JSON object from the model and parse the growing text into a
best-effort PARTIAL object after each chunk (the "streaming structured
output" pattern) — so a UI can render fields as they arrive rather than
waiting for the whole response. Uses the JSON repair machinery to close the
partial JSON on each step.
}
