% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cost.R
\name{estimate_prompt_cost}
\alias{estimate_prompt_cost}
\title{Estimate the USD cost of a request BEFORE sending it}
\usage{
estimate_prompt_cost(
  model = NULL,
  prompt = NULL,
  system = NULL,
  messages = NULL,
  tools = NULL,
  max_output_tokens = 0,
  registry = NULL,
  ...
)
}
\arguments{
\item{model}{Either a LanguageModelV1 object or a string id like
"openai:gpt-4o".}

\item{prompt}{A character prompt. Ignored when \code{messages} is supplied.}

\item{system}{Optional system prompt included in the count.}

\item{messages}{Optional pre-built message list (overrides \code{prompt}).}

\item{tools}{Optional list of Tool objects included in the count.}

\item{max_output_tokens}{Projected output tokens to price alongside the input
(default 0, i.e. input-only). Set it to your \code{max_tokens} for a worst case.}

\item{registry}{Optional ProviderRegistry to use.}

\item{...}{Additional model options passed to \code{\link[=count_tokens]{count_tokens()}}.}
}
\value{
An \code{aisdk_cost_estimate}: a list with \code{model_id}, \code{input_tokens},
\code{projected_output_tokens}, and \code{cost_usd} (\code{NA} when pricing is unknown).
}
\description{
A pre-flight budget check: counts the input tokens a prompt would use (exact
where the provider supports it — see \code{\link[=count_tokens]{count_tokens()}} — otherwise estimated)
and prices them, optionally adding a projected output. Unlike \code{\link[=estimate_cost]{estimate_cost()}}
which needs a completed response, this runs before the call so you can decide
whether to send it (e.g. cap per-request spend, or skip an over-budget batch).
}
\seealso{
\code{\link[=count_tokens]{count_tokens()}}, \code{\link[=estimate_cost]{estimate_cost()}}, \code{\link[=set_model_pricing]{set_model_pricing()}}
}
