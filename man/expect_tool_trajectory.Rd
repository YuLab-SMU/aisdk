% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agent_evals.R
\name{expect_tool_trajectory}
\alias{expect_tool_trajectory}
\title{Expect a Tool-Call Trajectory}
\usage{
expect_tool_trajectory(
  result,
  names,
  exact = FALSE,
  ordered = FALSE,
  info = NULL
)
}
\arguments{
\item{result}{A generation result from \code{generate_text()}.}

\item{names}{Character vector of expected tool names.}

\item{exact}{If TRUE, the called tool-name set must equal \code{names} exactly.}

\item{ordered}{If TRUE, \code{names} must appear as an ordered subsequence of the
actual call order (checks the agent called tools in the right order).}

\item{info}{Optional context for the failure message.}
}
\value{
Invisibly, a list with \code{passed} and the \code{actual} call names.
}
\description{
Assert on the sequence of tool calls a (possibly multi-step) agent made —
the trajectory, not just the final text. Reads the runtime's accumulated
\code{result$all_tool_calls}, so it works for multi-step agent runs.
}
