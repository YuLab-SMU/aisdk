% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agent_runtime.R
\name{set_run_trace_sink}
\alias{set_run_trace_sink}
\title{Set a sink for aisdk agent-runtime trace events}
\usage{
set_run_trace_sink(sink = NULL)
}
\arguments{
\item{sink}{A function \verb{function(event, run_id)}, or \code{NULL} to clear.}
}
\value{
Invisibly, the previously registered sink (or \code{NULL}).
}
\description{
Registers a function called once for every runtime lifecycle event
(\code{model_call}, \code{model_response}, \code{tool_results}, \code{policy_decision},
\code{network_error}, ...) as it happens. Each event is a list with \code{event_id},
\code{timestamp} (ISO-8601), \code{type}, and a typed \code{payload}; \code{model_response}
events additionally carry \code{model}, \code{provider}, \code{usage}, \code{response_id},
\code{latency_ms}, and \code{stream}. The sink is the seam for external observability
(e.g. mapping onto OpenTelemetry GenAI spans) and receives events even for
individual steps of a multi-step run, which the once-per-call hooks do not.
}
