% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cost.R
\name{estimate_cost}
\alias{estimate_cost}
\title{Estimate the USD cost of a generation}
\usage{
estimate_cost(usage, model_id)
}
\arguments{
\item{usage}{A usage list (as on \code{GenerateResult$usage}) with
\code{prompt_tokens}, \code{completion_tokens}, and optionally
\code{cache_read_input_tokens} / \code{cache_creation_input_tokens}.}

\item{model_id}{The model id used, optionally "provider:model".}
}
\value{
The estimated cost in USD (numeric), or \code{NA_real_} if the model's
pricing is unknown or usage is missing.
}
\description{
Computes cost from token usage and a per-model pricing table, accounting for
cached tokens: Anthropic reports \code{cache_read_input_tokens} /
\code{cache_creation_input_tokens} separately from \code{prompt_tokens}, and this
prices each at its own (discounted) rate.
}
