% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_api.R
\name{generate_text_fallback}
\alias{generate_text_fallback}
\title{Generate Text with Model Fallback}
\usage{
generate_text_fallback(prompt, models, ..., on_fallback = NULL)
}
\arguments{
\item{prompt}{The prompt (character or messages list).}

\item{models}{A non-empty list/vector of models (ids or objects) tried in order.}

\item{...}{Passed to every \code{generate_text()} call (system, tools, etc.).}

\item{on_fallback}{Optional \verb{function(failed_model, next_model, error)} called
before each fallover, e.g. to log it.}
}
\value{
The first successful \code{GenerateResult}. Errors with class
\code{aisdk_all_models_failed} if every model fails.
}
\description{
Try \code{generate_text()} with each model in turn, falling over to the next when
a model fails with a provider/API or network error (rate limit, outage,
auth, server, timeout) — the resilience pattern used by production agent
harnesses. A non-API error (e.g. a bug in a tool) is raised immediately, not
masked by a fallback.
}
