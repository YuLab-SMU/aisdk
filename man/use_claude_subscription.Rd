% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subscription_proxy.R
\name{use_claude_subscription}
\alias{use_claude_subscription}
\title{Use your Claude Code (Pro/Max) subscription via a local proxy}
\usage{
use_claude_subscription(
  name = "claude",
  base_url = NULL,
  port = NULL,
  model = NULL,
  api_key = "unused",
  supports_native_tools = TRUE,
  set_as_default = FALSE,
  check = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{name}{Provider id to register (referenced as \code{"<name>:<model>"}).}

\item{base_url}{Explicit proxy base URL. Highest priority. When \code{NULL}, aisdk
resolves the URL from an explicit \code{port}, then the \code{AISDK_CLAUDE_PROXY_URL}
env var, then the localhost default (\verb{http://127.0.0.1:3000/v1}).}

\item{port}{Localhost port of the proxy. When \code{NULL} (default), the env var
or the built-in default port is used. Ignored if \code{base_url} is given.}

\item{model}{Optional default model id (e.g. \code{"claude-sonnet-4-6"}). Only
used when \code{set_as_default = TRUE}.}

\item{api_key}{Placeholder credential sent to the proxy (default \code{"unused"}).
Most OAuth proxies ignore it and inject the real subscription token; set
this only if your proxy expects a shared secret.}

\item{supports_native_tools}{Whether the proxy supports native tool calling
(default \code{TRUE}). Set \code{FALSE} to fall back to text-embedded tool calls.}

\item{set_as_default}{If \code{TRUE}, set \code{"<name>:<model>"} as the session
default model (requires \code{model}).}

\item{check}{If \code{TRUE} (default), probe the proxy and warn if it is not
reachable.}

\item{quiet}{Suppress the one-time terms-of-service notice and the
confirmation message.}
}
\value{
Invisibly, a list with \code{provider}, \code{base_url}, \code{api_format}, and
\code{model}.
}
\description{
Registers an aisdk provider that routes requests to a locally running
Claude OAuth proxy (default: \code{claude-auth-proxy} at \verb{http://127.0.0.1:3000},
Anthropic Messages compatible), so calls bill against your Claude
subscription instead of API credits.
}
\examples{
\dontrun{
use_claude_subscription()
subscription_proxy_status("claude")
generate_text(model = "claude:claude-sonnet-4-6", prompt = "Hello")
}
}
\seealso{
\code{\link[=use_codex_subscription]{use_codex_subscription()}}, \code{\link[=subscription_proxy_status]{subscription_proxy_status()}},
\code{\link[=create_custom_provider]{create_custom_provider()}}
}
