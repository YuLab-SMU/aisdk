% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_api.R
\name{semantic_search}
\alias{semantic_search}
\title{Semantic Search over Documents}
\usage{
semantic_search(query, documents, model, top_k = 5L, registry = NULL)
}
\arguments{
\item{query}{The query string.}

\item{documents}{A character vector of documents to rank.}

\item{model}{An embedding model (id or object), e.g.
"openai:text-embedding-3-small".}

\item{top_k}{Number of top documents to return (default 5).}

\item{registry}{Optional ProviderRegistry.}
}
\value{
A data frame with \code{document}, \code{score} (cosine similarity), and
\code{index} (position in \code{documents}), ordered by descending score.
}
\description{
Rank a set of documents by semantic similarity to a query using an embedding
model — a self-contained retrieval primitive for RAG and just-in-time
context. Embeds the query and documents (one batched call) and returns the
top matches by cosine similarity.
}
