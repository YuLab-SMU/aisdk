% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subscription_proxy.R
\name{use_codex_subscription}
\alias{use_codex_subscription}
\title{Use your Codex (ChatGPT) subscription via a local proxy}
\usage{
use_codex_subscription(
  name = "codex",
  base_url = NULL,
  port = NULL,
  model = NULL,
  api_format = c("chat_completions", "responses"),
  api_key = "unused",
  supports_native_tools = TRUE,
  set_as_default = FALSE,
  check = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{name}{Provider id to register (referenced as \code{"<name>:<model>"}).}

\item{base_url}{Explicit proxy base URL. Highest priority. When \code{NULL}, aisdk
resolves the URL from an explicit \code{port}, then the \code{AISDK_CODEX_PROXY_URL}
env var, then the localhost default (\verb{http://127.0.0.1:10531/v1}).}

\item{port}{Localhost port of the proxy. When \code{NULL} (default), the env var
or the built-in default port is used. Ignored if \code{base_url} is given.}

\item{model}{Optional default model id (e.g. \code{"gpt-5.4-codex"}). Only used
when \code{set_as_default = TRUE}.}

\item{api_format}{Wire format the proxy speaks: \code{"chat_completions"}
(default) or \code{"responses"}.}

\item{api_key}{Placeholder credential sent to the proxy (default \code{"unused"}).
Most OAuth proxies ignore it and inject the real subscription token; set
this only if your proxy expects a shared secret.}

\item{supports_native_tools}{Whether the proxy supports native tool calling
(default \code{TRUE}). Set \code{FALSE} to fall back to text-embedded tool calls.}

\item{set_as_default}{If \code{TRUE}, set \code{"<name>:<model>"} as the session
default model (requires \code{model}).}

\item{check}{If \code{TRUE} (default), probe the proxy and warn if it is not
reachable.}

\item{quiet}{Suppress the one-time personal-use notice and the confirmation
message.}
}
\value{
Invisibly, a list with \code{provider}, \code{base_url}, \code{api_format}, and
\code{model}.
}
\description{
Registers an aisdk provider that routes requests to a locally running
Codex OAuth proxy (default: \code{openai-oauth} at \verb{http://127.0.0.1:10531/v1}),
so calls bill against your ChatGPT subscription instead of API credits.
}
\examples{
\dontrun{
use_codex_subscription()
subscription_proxy_status("codex")
generate_text(model = "codex:gpt-5.4-codex", prompt = "Hello")
}
}
\seealso{
\code{\link[=use_claude_subscription]{use_claude_subscription()}}, \code{\link[=subscription_proxy_status]{subscription_proxy_status()}},
\code{\link[=create_custom_provider]{create_custom_provider()}}
}
