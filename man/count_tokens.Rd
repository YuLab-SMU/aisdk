% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_api.R
\name{count_tokens}
\alias{count_tokens}
\title{Count Input Tokens}
\usage{
count_tokens(
  model = NULL,
  prompt = NULL,
  system = NULL,
  messages = NULL,
  tools = NULL,
  registry = NULL,
  ...
)
}
\arguments{
\item{model}{Either a LanguageModelV1 object or a string ID like
"anthropic:claude-sonnet-5".}

\item{prompt}{A character prompt to count. Ignored when \code{messages} is given.}

\item{system}{Optional system prompt included in the count.}

\item{messages}{Optional pre-built message list (overrides \code{prompt}).}

\item{tools}{Optional list of Tool objects included in the count (tool
definitions consume input tokens).}

\item{registry}{Optional ProviderRegistry to use.}

\item{...}{Additional model options (e.g. \code{thinking}).}
}
\value{
An integer token count.
}
\description{
Count the input tokens a prompt (or full message list) would use for a model,
as a pre-flight check before sending — e.g. to verify it fits the context
window or to estimate cost. Providers with a native counting endpoint
(Anthropic) return an exact count; others use a local heuristic estimate.
}
\examples{
\donttest{
if (interactive()) {
  n <- count_tokens("anthropic:claude-sonnet-5", "Summarize the R language.")
  print(n)
}
}
}
